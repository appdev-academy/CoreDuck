# References:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
osx_image: xcode8

before_install:
- gem install cocoapods --no-ri --no-rdoc

script:
- set -o pipefail && xcodebuild -workspace Example/CoreDuck.xcworkspace -scheme CoreDuck-iOS -sdk iphonesimulator10.0 ONLY_ACTIVE_ARCH=NO | xcpretty
- set -o pipefail && xcodebuild -workspace Example/CoreDuck.xcworkspace -scheme CoreDuck-macOS -sdk macosx ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
