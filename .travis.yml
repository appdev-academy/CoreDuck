# References:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: swift
osx_image: xcode10

script:
- set -o pipefail && xcodebuild -workspace Example/CoreDuck.xcworkspace -scheme CoreDuck-iOS -sdk iphonesimulator12.0 ONLY_ACTIVE_ARCH=NO | xcpretty
- set -o pipefail && xcodebuild -workspace Example/CoreDuck.xcworkspace -scheme CoreDuck-macOS -sdk macosx10.14 ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
